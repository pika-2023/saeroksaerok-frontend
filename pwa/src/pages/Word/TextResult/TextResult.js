import React from "react";
import styled from "styled-components";

const TextResult = () => {
  return (
    <TextResultContainer>
      <TextResultTitle>TextResult</TextResultTitle>
    </TextResultContainer>
  );
};

export default TextResult;

const TextResultContainer = styled.div``;

const TextResultTitle = styled.div``;
